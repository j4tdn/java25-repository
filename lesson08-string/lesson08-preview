A. nhắc lại khái niệm
- final: final cho biến
 --> biến có từ khóa final thì mình sẽ không thể dùng toán tủw = để gán lại giá trị( tại STACK)
 --> final ở Stack
 
 - immutable: immutable cho object
 --> object được gọi là immutable khi object đó được tạo ra và lưu trữ tại vùng nhớ HEAP sau đó 
 	không thể cập nhật giá trị --> immutable object
 	
 --> để object có thể là immutable thì 
 	- class của nó phải có private attribute(s) và final cho attributes
 	- nâng cao: private và final cho các attribue thì các attribute đó vẫn có thể cập nhật được
 	
 	Item
		private final Integer id; 
		private final String name; 
		private final Order order;
		
		private Integer getId() {
			return id;
		｝
		private String getName () {
			return name;
		}
		private Order getOrder () {
			return Order;
		}
		
	App --> Item item = new Item()
			Order order = item.getOrder();
			Order setTotalOfMoney(125);